document.addEventListener("DOMContentLoaded", function(event) {
  let fabric = new FabricObject("canvas");
  // fabric.testAdd();
  fabric.render();
  // console.log(fabric.toSvg());
fabr = fabric;
});
var fabr;
